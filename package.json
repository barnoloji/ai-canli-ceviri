{
  "name": "live-translation-app",
  "version": "1.0.0",
  "description": "Real-time conference translation system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd live-translation-frontend && npm install && npm run build",
    "install-all": "npm install && cd live-translation-frontend && npm install",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}